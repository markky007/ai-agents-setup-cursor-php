---
description: DevOps, CI/CD, Docker, and deployment configuration work
globs: "**/Dockerfile,**/docker-compose*.yml,**/.gitlab-ci.yml,**/.gitlab/**/*.yml,**/azure-pipelines*.yml,**/.github/workflows/**,**/k8s/**,**/*.env.example,**/helm/**,**/php.ini,**/www.conf,**/supervisord.conf,**/octane.php"
alwaysApply: false
---

# DevOps, CI/CD & Deployment

When modifying DevOps, CI/CD, Docker, environment, or deployment config:

1. Inspect the existing pipeline and deployment flow first
2. Preserve current build/test/lint/package/deploy **stages and variable names** unless change is explicitly required
3. Identify required environment variables
4. Ensure secrets are never committed, logged, or copied into images
5. Prefer deterministic versions and reproducible builds (`composer.lock`)
6. Validate local and CI behavior where possible
7. Consider rollback, health checks, readiness checks, and deployment order
8. Document required runtime configuration
9. Report deployment risks clearly

Do not introduce steps that can leave the system partially broken. Prefer smallest safe config change (Ponytail) without skipping security or rollback considerations.

Do **not** guess php-fpm vs Octane vs FrankenPHP — read the Dockerfile and process manager config that already exist.

**Do:** pin base images; run `composer install --no-dev --optimize-autoloader` in production stages when that is already the pattern.

**Don't:** rename existing CI variables; reorder/delete stages; set `APP_DEBUG=true` in production images; put `.env` with secrets in the image.

## Observability & logging

- Use existing logger patterns (`Log` / structured logging already in the app)
- Never log secrets, tokens, passwords, personal data, or sensitive payloads
- Errors observable without exposing internals to users
- Add metrics/tracing only when useful and consistent with existing infrastructure
